# CODEOWNERS
#
# What this file would normally do: ask GitHub to auto-request a review from
# a named person or team on matching paths. It does NOT gate a merge here —
# see PULL_REQUEST_TEMPLATE.md: this repository is generated by an export
# from a private repo, so no PR is merged into it directly regardless of who
# reviews it. Its only value in this repository is routing triage — making
# sure a proposed change or issue reaches the right eyes quickly.
#
# The handle below was supplied by the operator, not inferred. This repository
# has a standing rule against inventing a contact that does not exist (the same
# rule that kept SECURITY.md from shipping a placeholder email), because a
# fabricated @handle routes nothing while looking configured.

*  @bastiancarmy
